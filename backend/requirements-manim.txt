# Optional: local Manim rendering for /process-data (needs system libs: pangocairo, ffmpeg, etc.)
# Not used on Vercel — install only on a full dev machine:
#   pip install -r requirements.txt -r requirements-manim.txt

-r requirements.txt

manim>=0.19.0
numpy>=1.24.0
pillow>=10.0.0
pycairo>=1.23.0
pygments>=2.16.1
tqdm>=4.66.1
